---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netboot-xyz
  namespace: network
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
