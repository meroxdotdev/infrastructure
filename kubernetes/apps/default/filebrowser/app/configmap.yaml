---
apiVersion: v1
kind: ConfigMap
metadata:
  name: filebrowser-config
  namespace: default
data:
  config.yaml: |
    server:
      port: 80
      cacheDir: /home/filebrowser/data/tmp
      sources:
        - path: /srv/media
          name: media
          config:
            defaultEnabled: true
        - path: /srv/backups
          name: backups
          config:
            defaultEnabled: true
        - path: /srv/photos
          name: photos
          config:
            defaultEnabled: true
        - path: /srv/isos
          name: isos
          config:
            defaultEnabled: true
        - path: /srv/documents
          name: documents
          config:
            defaultEnabled: true
