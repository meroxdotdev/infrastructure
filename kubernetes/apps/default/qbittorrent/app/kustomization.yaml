apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
- files:
  - qbittorrent.yaml=./lokirule.yaml
  name: qbittorrent-loki-rules
  options:
    labels:
      loki_rule: "true"
generatorOptions:
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
  disableNameSuffixHash: true
kind: Kustomization
namespace: default
resources:
- ./helmrelease.yaml
- ./pvc.yaml
- ./gluetun-secret.sops.yaml
