---
apiVersion: v1
kind: Service
metadata:
  name: proxmox
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
      name: https
  selector: {}  # Empty selector since we'll use Endpoints
