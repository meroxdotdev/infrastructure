---
# docker_setup runs once, at the start of site.yml's first play - declaring
# it here too caused it to re-run ~9 times across the playbook, repeatedly
# rewriting /etc/docker/daemon.json and re-notifying "restart docker" after
# service containers already existed, wiping them out.
dependencies: []
