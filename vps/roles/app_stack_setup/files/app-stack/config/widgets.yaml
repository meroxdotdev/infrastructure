---
- datetime:
    text_size: xl
    format:
      dateStyle: long
      timeStyle: short
      hour12: false
      locale: ro-RO

- kubernetes:
    cluster:
      show: true
      cpu: true
      memory: true
      showLabel: true
      label: "k8s"
    nodes:
      show: false

# Disabled 2026-07-27 - provider URL (10.57.57.80:30887) refuses connections
# from the VPS (confirmed: connection refused, not a DNS issue this time).
# Was showing a permanent "API Error" card at the very top of the page.
# Re-enable once the real Longhorn UI address/port is confirmed.
# - longhorn:
#     expanded: true
#     total: true
#     labels: true
#     nodes: false
