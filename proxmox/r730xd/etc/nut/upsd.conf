# Empty by default, which means upsd binds 127.0.0.1 only and a network slave
# can never connect. Bind the LAN address explicitly rather than 0.0.0.0: the
# only client is px-0, and this host also carries a Tailscale interface that
# has no business serving UPS status.
LISTEN 10.57.57.250 3493
