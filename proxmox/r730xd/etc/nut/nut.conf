# Was `standalone` until 2026-09-01. px-0 is on the same UPS but has no USB
# connection to it, so it needs to read status over the network — which upsd
# only serves in netserver mode. See proxmox/px-0/etc/nut/upsmon.conf.
MODE=netserver
